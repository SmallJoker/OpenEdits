This directory contains imported (server) and exported (client) worlds.

imports/
	^ .eelvl files to import using the server-side /import command
	Files are searched recursively but hidden directories (e.g. `.test/`) are ignored.

exports/
	^ .eelvl files exported using the client-side .export command
